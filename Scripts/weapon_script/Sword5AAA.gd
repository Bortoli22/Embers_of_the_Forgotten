extends Area2D
var damageValue = 140
var staggerWindow = 0 #x seconds
var animations = ["5AAA startup", "5AAA active", "5AAA recovery"]

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
